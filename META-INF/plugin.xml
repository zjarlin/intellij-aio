<idea-plugin>
  <change-notes><![CDATA[<h1>Changelog</h1>

<p>All notable changes to Gradle Buddy plugin will be documented in this file.</p>

<h2>[2026.01.26] - 2026-01-26</h2>

<h3>✨ 新增功能</h3>
<ul><li><strong>Select other versions（KTS/TOML）</strong>：在 <code>.gradle.kts</code> 和 <code>libs.versions.toml</code> 中选择任意版本并替换<ul><li>KTS 支持硬编码依赖和 <code>libs.xxx.yyy</code> 引用</li><li>TOML 支持 <code>module</code> / <code>group+name</code> / <code>version.ref</code> / 直接 <code>version</code> 格式</li></ul></li><li><strong>Catalog -&gt; Hardcoded</strong>：新增意图将 <code>implementation(libs.xxx.yyy)</code> 转为硬编码依赖字符串</li></ul>

<h3>🔧 改进</h3>
<ul><li><strong>版本目录解析</strong>：支持扫描多模块下的 <code>gradle/*.versions.toml</code>，并优先使用设置中的路径</li><li><strong>TOML 解析</strong>：依赖行支持尾部注释</li></ul>

<hr />

<h2>[2026.01.25] - 2026-01-25</h2>

<h3>✨ 新增功能</h3>
<ul><li><strong>版本选择对话框</strong>：统一的版本选择 UI，用于 “Select other versions”</li></ul>

<hr />

<h2>[2026.01.24] - 2026-01-24</h2>

<h3>🐛 修复</h3>
<ul><li><strong>KTS 引用格式</strong>：将硬编码依赖转换为 TOML 后，KTS 引用使用点号访问（如 <code>libs.hutool.all</code>）</li></ul>

<hr />

<h2>[Unreleased] - 2025-01-23</h2>

<h3>✨ 新增功能</h3>
<ul><li><strong>版本目录引用修复器</strong>：智能检测和修复无效的版本目录引用<ul><li><strong>智能相似度匹配</strong>：使用多因子评分算法（完全匹配 50%、Jaccard 相似度 30%、顺序相似度 20%）查找所有匹配的候选项</li><li><strong>无限制候选项</strong>：显示所有至少有一个 token 匹配的别名，按相似度排序</li><li><strong>浏览替代项</strong>：新增意图操作，即使引用有效也可以浏览其他候选项</li><li><strong>光标位置无关</strong>：无论光标在表达式的哪个位置，都能提取完整的 token 列表</li><li><strong>上下文菜单 UI</strong>：智能弹出菜单显示候选项，包含匹配百分比和匹配的关键词</li><li><strong>当前引用标识</strong>：在替代项列表中用 &quot;✓ 当前&quot; 标记当前使用的引用</li><li><strong>多模块支持</strong>：递归扫描项目中所有模块的 TOML 文件</li></ul></li></ul>

<h3>🔧 改进</h3>
<ul><li><strong>所有意图操作</strong>：添加 <code>(Gradle Buddy)</code> 前缀和英文描述，便于识别插件来源<ul><li><code>(Gradle Buddy) Select correct catalog reference (N candidates)</code> - 选择正确的版本目录引用</li><li><code>(Gradle Buddy) Browse catalog alternatives (N candidates)</code> - 浏览其他版本目录引用</li><li><code>(Gradle Buddy) Fix build-logic qualified name</code> - 修复 build-logic 限定名</li><li><code>(Gradle Buddy) Convert plugin to version catalog format (TOML)</code> - 将插件转换为版本目录格式</li><li><code>(Gradle Buddy) Convert dependency to version catalog format (TOML)</code> - 将依赖转换为版本目录格式</li><li><code>(Gradle Buddy) Update dependency to latest version</code> - 更新依赖到最新版本</li></ul></li></ul>

<h3>🐛 修复</h3>
<ul><li><strong>Token 提取 Bug</strong>：修复光标位置影响 token 提取的问题<ul><li>现在无论光标在哪个位置，都能找到最顶层的 <code>KtDotQualifiedExpression</code></li><li>确保提取所有 token 用于相似度匹配（例如：<code>libs.bcprov.jdk15to18</code> → <code>[bcprov, jdk15to18]</code>）</li></ul></li><li><strong>资源文件位置</strong>：将意图操作描述文件从子模块移动到主模块资源目录<ul><li>修复 IDE 中意图操作不显示的问题</li><li>所有 <code>intentionDescriptions</code> 现在位于 <code>plugins/gradle-buddy/src/main/resources/</code></li></ul></li></ul>

<h3>📝 文档</h3>
<ul><li>为新的版本目录引用修复器功能添加了完整文档<ul><li><code>SIMILARITY_MATCHING_EXAMPLES.md</code>：算法概述和评分示例</li><li><code>BUG_FIX_SUMMARY.md</code>：详细的 bug 修复说明</li><li><code>RESOURCE_FIX.md</code>：资源文件位置修复指南</li><li><code>INTENTION_TEXT_UPDATE.md</code>：意图操作文本更新总结</li><li><code>CHANGELOG_LATEST.md</code>：最新改动总结</li></ul></li></ul>

<h3>🎯 使用场景</h3>
<ol><li><p><strong>修复无效引用</strong>：当 <code>libs.com.google.devtools.ksp.gradle.plugin</code> 在 TOML 中不存在时</p><ul><li>显示所有包含 <code>com</code>、<code>google</code>、<code>devtools</code>、<code>ksp</code>、<code>gradle</code>、<code>plugin</code> 的候选项</li><li>示例：<code>gradle.plugin.ksp</code>（85% 匹配：gradle, plugin, ksp）</li></ul></li><li><p><strong>浏览替代项</strong>：即使 <code>libs.gradle.plugin.ksp</code> 是有效引用</p><ul><li>在引用的任意位置按 <code>Alt+Enter</code></li><li>查看 TOML 中所有相关的依赖项</li><li>当前引用标记为 &quot;✓ 当前&quot;</li><li>轻松切换到其他版本或变体</li></ul></li></ol>

<hr />

<h2>[Unreleased] - 2025-01-22</h2>

<h3>✨ Added</h3>
<ul><li><strong>Plugin ID 修复工具</strong>：自动修复 build-logic 预编译脚本插件的 ID 引用<ul><li>新增 <code>FixPluginIdIntention</code>：Alt+Enter 快速修复单个插件 ID</li><li>新增 <code>FixAllPluginIdsAction</code>：批量修复项目中所有插件 ID</li><li>新增 <code>PluginIdScanner</code>：递归扫描 build-logic 目录，提取插件元数据</li><li>新增 <code>IdReplacementEngine</code>：查找和替换插件 ID 引用</li><li>支持自动提取 Kotlin 文件的包名</li><li>支持嵌套的 build-logic 目录结构</li><li>线程安全的 PSI 访问（所有操作都包装在 ReadAction 中）</li><li>进度指示器和详细的操作结果通知</li></ul></li><li><strong>id-fixer 模块</strong>：独立的插件 ID 修复模块<ul><li><code>PluginIdInfo</code>：插件元数据数据类</li><li><code>ReplacementCandidate</code>：替换候选位置</li><li><code>ReplacementResult</code>：替换操作结果</li><li>完整的文档和使用示例</li></ul></li></ul>

<h3>🐛 Fixed</h3>
<ul><li>修复 <code>PluginIdScanner.extractPackageName()</code> 的线程安全问题<ul><li>将 PSI 访问移到 ReadAction.compute 块内</li><li>避免在后台线程中直接访问 PSI 元素</li></ul></li></ul>

<h3>📝 Documentation</h3>
<ul><li>新增 <code>id-fixer/README.md</code>：详细的功能说明和架构文档</li><li>新增 <code>id-fixer/CHANGELOG.md</code>：模块变更记录</li><li>更新主 README 添加 Plugin ID 修复工具说明</li><li>新增使用示例和问题背景说明</li></ul>

<hr />

<h2>[Unreleased] - 2025-12-07</h2>

<h3>✨ Added</h3>
<ul><li><strong>递归依赖推导</strong>：自动分析并加载模块的所有依赖模块<ul><li>支持 <code>project(&quot;:path:to:module&quot;)</code> 格式</li><li>支持 <code>projects.path.to.module</code> Type-safe accessors 格式</li><li>使用 BFS 算法避免循环依赖</li><li>自动忽略注释掉的依赖声明</li></ul></li><li><strong>依赖配置支持</strong>：支持所有 Gradle 依赖配置类型<ul><li><code>implementation</code>, <code>api</code>, <code>compileOnly</code>, <code>runtimeOnly</code></li><li><code>testImplementation</code>, <code>testCompileOnly</code>, <code>testRuntimeOnly</code></li><li><code>annotationProcessor</code>, <code>kapt</code>, <code>ksp</code></li></ul></li><li><strong>Gradle 插件版本更新</strong>：在 settings.gradle.kts 中支持插件版本更新<ul><li>支持 <code>id(&quot;plugin.id&quot;) version &quot;version&quot;</code> 格式</li><li>自动从 Gradle Plugin Portal 查询最新版本</li><li>与依赖版本更新使用相同的 Alt+Enter 意图操作</li></ul></li><li><strong>字符串工具类</strong>：新增 <code>StringUtils</code> 替代 dataframe 依赖<ul><li><code>toCamelCaseByDelimiters()</code>: 转换为 camelCase</li><li><code>toKebabCase()</code>: 转换为 kebab-case</li></ul></li><li><strong>JVM 库工具类</strong>：新增 <code>GradlePluginSearchUtil</code> 到 addzero-lib-jvm<ul><li>查询 Gradle 插件最新版本</li><li>从 Gradle Plugin Portal API 获取数据</li></ul></li><li><strong>测试覆盖</strong>：新增 <code>OnDemandModuleLoaderTest</code> 单元测试</li><li><strong>文档完善</strong>：新增 <code>RECURSIVE_DEPENDENCY_DETECTION.md</code> 详细说明递归依赖推导原理</li></ul>

<h3>🔧 Changed</h3>
<ul><li><code>detectModulesFromOpenFiles()</code> 现在返回包含递归依赖的完整模块集合</li><li><code>OnDemandModuleLoader</code> 新增私有方法：<ul><li><code>expandWithDependencies()</code>: 递归展开模块及其依赖</li><li><code>extractProjectDependencies()</code>: 从 build.gradle.kts 提取依赖</li><li><code>findBuildFile()</code>: 查找模块的构建文件</li><li><code>parseProjectDependencies()</code>: 解析依赖声明（支持两种格式）</li></ul></li></ul>

<h3>🐛 Fixed</h3>
<ul><li>修复按需加载时可能遗漏传递依赖导致编译失败的问题</li><li>修复注释掉的依赖被错误解析的问题</li><li>修复缺失 <code>org.jetbrains.kotlinx.dataframe</code> 依赖导致的二进制不兼容问题<ul><li>移除了对 dataframe 库的依赖</li><li>使用自实现的字符串工具函数替代</li></ul></li><li>修复 <code>URL(String)</code> 已弃用 API 使用警告<ul><li>使用 <code>URI.toURL()</code> 替代废弃的构造函数</li><li>兼容未来版本的 Java API 变更</li></ul></li></ul>

<h3>📝 Documentation</h3>
<ul><li>更新 README 添加递归依赖推导功能说明</li><li>新增依赖格式示例和使用场景说明</li></ul>

<hr />

<h2>[2025.11.31] - 2025-11-30</h2>

<h3>🎯 解决的痛点</h3>
<ul><li><strong>Gradle Sync 慢</strong>：大型多模块项目 Sync 需要 5-10 分钟，现在只需 30 秒</li><li><strong>内存占用高</strong>：100 个模块全加载占用 8GB+，现在只加载用到的模块</li><li><strong>手动管理麻烦</strong>：不再需要手动注释 settings.gradle.kts</li></ul>

<h3>✨ Added</h3>
<ul><li><strong>StatusBarWidgetFactory</strong>：使用官方稳定 API 注册状态栏组件，替代已废弃的 <code>addWidget()</code> 方法</li><li><strong>模块排除统计</strong>：通知消息显示 <code>Loaded: X, Excluded: Y, Total: Z</code></li><li><strong>构建模块自动排除</strong>：自动排除 <code>build-logic</code>、<code>buildSrc</code>、<code>buildLogic</code> 等构建基础设施模块</li><li><strong>settings.gradle.kts 注释增强</strong>：生成的配置块包含统计信息和被排除模块列表</li></ul>

<h3>🔧 Changed</h3>
<ul><li><strong>GradleBuddyService 实现 Disposable</strong>：替代实验性的 <code>ProjectCloseListener</code>，使用稳定的生命周期管理</li><li><strong>partitionModules() 函数</strong>：分离有效模块和被排除模块，便于统计和展示</li></ul>

<h3>🗑️ Removed</h3>
<ul><li><strong>GradleBuddyProjectManagerListener</strong>：不再需要，由 Disposable 模式替代</li><li><strong>实验性 API 依赖</strong>：移除 <code>ProjectCloseListener</code> 的使用</li></ul>

<h3>🐛 Fixed</h3>
<ul><li>修复 <code>StatusBar.addWidget(StatusBarWidget)</code> 已弃用警告</li><li>修复 <code>ProjectCloseListener</code> 实验性 API 警告</li></ul>

<hr />

<h2>[2025.11.32] - 2025-11-30</h2>

<h3>✨ Added</h3>
<ul><li><strong>按需模块加载</strong>：只加载当前打开的编辑器标签页对应的模块</li></ul>

<h3>✨ 意图操作 (Alt+Enter)</h3>
<ul><li><strong>Update dependency to latest version</strong>：在依赖声明上按 <code>Alt+Enter</code>，自动从 Maven Central 获取最新版本</li></ul>

<h3>🔄 迁移工具</h3>
<ul><li><strong>Version Catalog 迁移</strong>：扫描所有 <code>.gradle.kts</code> 文件，将硬编码依赖迁移到 <code>gradle/libs.versions.toml</code></li><li><strong>项目依赖迁移</strong>：将 <code>project(&quot;:module&quot;)</code> 依赖迁移到 Maven 坐标</li></ul>

<h3>📝 Documentation</h3>
<ul><li>完整的 README 文档，包含痛点说明和使用方法</li><li>详细的功能介绍：工具窗口、意图操作、迁移工具</li><li>代码示例：Version Catalog 迁移前后对比</li></ul>]]></change-notes>
  <version>2026.02.04</version>
  <id>site.addzero.gradle-buddy</id>
  <name>gradle-buddy</name>
  <vendor email="zjarlin@outlook.com">zjarlin</vendor>
  <!-- K2 Compatibility Declaration -->
  <idea-version since-build="252" until-build="262.*" />
  <description><![CDATA[<h2># Gradle Buddy provides efficient task management and dependency tools for multi-module projects, helping you work smarter and faster.</h2>
<h2>功能特性</h2>

<h3>🆕 近期更新 (2026.01.24 - 2026.01.26)</h3>
<ul><li><strong>Select other versions</strong>：在 KTS/TOML 中自由选择版本并替换</li><li><strong>Catalog -&gt; Hardcoded</strong>：将 <code>libs.xxx.yyy</code> 一键转为硬编码依赖</li><li><strong>版本目录解析增强</strong>：支持多模块下的 <code>gradle/*.versions.toml</code></li><li><strong>KTS 引用修复</strong>：转换为 TOML 后使用点号访问（如 <code>libs.hutool.all</code>）</li></ul>

<h3>🚀 模块面板功能</h3>
<ul><li><strong>当前模块任务窗口</strong>：右侧边栏显示当前编辑器文件所属模块的 Gradle 任务，双击即可运行</li></ul>

<h3>✨ 意图操作 (Alt+Enter)</h3>

<p>本插件提供了一系列意图操作，让你在 <code>.gradle.kts</code> 和 <code>libs.versions.toml</code> 文件中更高效地管理依赖和插件。</p>

<p>所有意图操作都带有 <code>(Gradle Buddy)</code> 前缀，方便识别插件来源。</p>

<hr />

<h4>意图操作汇总（按文件类型）</h4>

<p><strong>.gradle.kts / settings.gradle.kts</strong></p>

<table><thead><tr><th>意图</th><th>说明</th><th>支持范围</th></tr></thead><tbody><tr><td>Update dependency to latest version</td><td>查询最新版本并替换</td><td>依赖与插件版本</td></tr><tr class="intellij-row-even"><td>Convert dependency to version catalog format (TOML)</td><td>硬编码依赖转为 TOML 引用</td><td>硬编码依赖</td></tr><tr><td>Convert catalog reference to hardcoded dependency</td><td><code>libs.xxx.yyy</code> 转硬编码</td><td>版本目录引用</td></tr><tr class="intellij-row-even"><td>Select correct catalog reference</td><td>智能修复无效引用</td><td>版本目录引用</td></tr><tr><td>Browse catalog alternatives</td><td>浏览并切换候选项</td><td>版本目录引用</td></tr><tr class="intellij-row-even"><td>Select other versions</td><td>选择指定版本并替换</td><td>硬编码依赖、版本目录引用</td></tr></tbody></table>

<p><strong>libs.versions.toml</strong></p>

<table><thead><tr><th>意图</th><th>说明</th><th>支持范围</th></tr></thead><tbody><tr><td>Update dependency to latest version</td><td>查询最新版本并替换</td><td>[libraries] 依赖声明</td></tr><tr class="intellij-row-even"><td>Update version variable to latest</td><td>更新 [versions] 变量</td><td>[versions]</td></tr><tr><td>Select other versions</td><td>选择指定版本并替换</td><td>[libraries] 依赖声明</td></tr></tbody></table>

<hr />

<h4>在 <code>.gradle.kts</code> 或 <code>settings.gradle.kts</code> 文件中</h4>

<p>将光标置于依赖或插件声明上，按下 <code>Alt+Enter</code>，即可触发以下操作：</p>

<p><strong>1. (Gradle Buddy) Update to latest version (更新到最新版本)</strong></p>

<ul><li><strong>痛点</strong>：想升级依赖或插件，但不确定最新版本号，需要手动去 Maven Central 或 Gradle Plugin Portal 查询。</li><li><strong>解决</strong>：自动查询并替换为最新稳定版。</li></ul>

<p><em>示例 (依赖)</em>:</p>
<pre><code class="language-kotlin">// 更新前
implementation(&quot;com.google.guava:guava:31.0-jre&quot;)

// 更新后
implementation(&quot;com.google.guava:guava:33.2.1-jre&quot;)
</code></pre>

<p><em>示例 (插件)</em>:</p>
<pre><code class="language-kotlin">// 更新前
plugins {
    id(&quot;org.jetbrains.kotlin.jvm&quot;) version &quot;1.8.0&quot;
}

// 更新后
plugins {
    id(&quot;org.jetbrains.kotlin.jvm&quot;) version &quot;1.9.23&quot;
}
</code></pre>

<p><strong>2. (Gradle Buddy) Convert to version catalog (转换为版本目录)</strong></p>

<ul><li><strong>痛点</strong>：项目中存在硬编码的依赖和插件版本，不便于统一管理。</li><li><strong>解决</strong>：一键将硬编码的声明转换为 <code>libs.versions.toml</code> 中的引用。</li></ul>

<p><em>示例 (依赖)</em>:</p>
<pre><code class="language-kotlin">// 转换前
implementation(&quot;com.google.guava:guava:31.0-jre&quot;)

// 转换后 (自动在 toml 创建条目)
implementation(libs.guava)
</code></pre>

<p><em>示例 (插件)</em>:</p>
<pre><code class="language-kotlin">// 转换前
plugins {
    id(&quot;org.jetbrains.kotlin.jvm&quot;) version &quot;1.8.0&quot;
}

// 转换后 (自动在 toml 创建条目)
plugins {
    alias(libs.plugins.kotlin.jvm)
}
</code></pre>

<p><strong>3. (Gradle Buddy) Select correct catalog reference (选择正确的版本目录引用)</strong> 🆕</p>

<ul><li><strong>痛点</strong>：版本目录引用写错了，但不知道 TOML 中正确的引用是什么。</li><li><strong>解决</strong>：使用智能相似度匹配，显示所有可能的候选项，按匹配度排序。</li></ul>

<p><em>示例</em>:</p>
<pre><code class="language-kotlin">// 错误的引用
implementation(libs.com.google.devtools.ksp.gradle.plugin)
// 光标放在任意位置，按 Alt+Enter

// 显示候选项（按相似度排序）：
// 1. gradle.plugin.ksp [85%] (匹配: gradle, plugin, ksp)
// 2. ksp.gradle.plugin [75%] (匹配: ksp, gradle, plugin)
// 3. google.ksp [45%] (匹配: google, ksp)
</code></pre>

<p><strong>特性</strong>：</p>
<ul><li>🎯 <strong>智能匹配</strong>：使用多因子评分算法（完全匹配 50%、集合相似度 30%、顺序相似度 20%）</li><li>📊 <strong>显示所有候选项</strong>：不限制数量，显示所有有关键词匹配的别名</li><li>🔍 <strong>光标位置无关</strong>：无论光标在 <code>gradle</code>、<code>plugin</code> 还是 <code>ksp</code> 上，都能提取完整的 token 列表</li><li>📈 <strong>匹配度显示</strong>：每个候选项显示匹配百分比和匹配的关键词</li><li>🌐 <strong>多模块支持</strong>：递归扫描所有模块的 TOML 文件</li></ul>

<p><strong>4. (Gradle Buddy) Browse catalog alternatives (浏览其他版本目录引用)</strong> 🆕</p>

<p><strong>5. (Gradle Buddy) Select other versions (选择其他版本)</strong> 🆕</p>

<ul><li><strong>痛点</strong>：需要切换到指定版本，不一定是最新版本。</li><li><strong>解决</strong>：从 Maven Central 拉取历史版本列表并手动选择。</li></ul>

<p><strong>6. (Gradle Buddy) Convert catalog reference to hardcoded dependency (转为硬编码)</strong> 🆕</p>

<ul><li><p><strong>痛点</strong>：临时测试或对比时，希望把 <code>libs.xxx.yyy</code> 转为硬编码。</p></li><li><p><strong>解决</strong>：自动解析 TOML 并替换为 <code>group:artifact:version</code>。</p></li><li><p><strong>痛点</strong>：想看看 TOML 中还有哪些相关的依赖可以用，但不想手动翻 TOML 文件。</p></li><li><p><strong>解决</strong>：即使当前引用有效，也可以浏览所有相关的候选项。</p></li></ul>

<p><em>示例</em>:</p>
<pre><code class="language-kotlin">// 有效的引用
implementation(libs.gradle.plugin.ksp)
// 光标放在任意位置，按 Alt+Enter

// 显示所有相关候选项：
// 1. gradle.plugin.ksp [100%] ✓ 当前
// 2. ksp.gradle.plugin [85%] (匹配: ksp, gradle, plugin)
// 3. gradle.ksp [75%] (匹配: gradle, ksp)
</code></pre>

<p><strong>特性</strong>：</p>
<ul><li>✅ <strong>当前引用标识</strong>：用 &quot;✓ 当前&quot; 标记正在使用的引用</li><li>🔄 <strong>快速切换</strong>：轻松切换到其他版本或变体</li><li>🔍 <strong>探索相关依赖</strong>：发现 TOML 中所有包含相同关键词的依赖</li></ul>

<hr />

<h4>在 <code>libs.versions.toml</code> 文件中</h4>

<p>将光标置于 TOML 文件中的任意位置，按下 <code>Alt+Enter</code>，即可触发以下操作：</p>

<p><strong>1. (Gradle Buddy) Update to latest version (更新到最新版本)</strong></p>

<ul><li><strong>痛点</strong>：即使在使用版本目录，依然需要手动检查每个依赖的最新版本。</li><li><strong>解决</strong>：将光标放在依赖声明上，即可自动更新到最新版本。</li></ul>

<p><em>示例</em>:</p>
<pre><code class="language-toml">[versions]
# 将光标放在 &quot;jupiter&quot; 版本号上，或在下面的 libraries 定义上
jupiter = &quot;5.9.1&quot;

[libraries]
# 将光标放在这一行
junit-jupiter-api = { group = &quot;org.junit.jupiter&quot;, name = &quot;junit-jupiter-api&quot;, version.ref = &quot;jupiter&quot; }
</code></pre>

<p><strong>2. Organize Version Catalog (整理版本目录)</strong></p>

<ul><li><strong>痛点</strong>：<code>libs.versions.toml</code> 文件内容一多就变得混乱，手动分组和排序费时费力。</li><li><strong>解决</strong>：一键格式化整个 TOML 文件，使其规整、有序、易于维护。</li><li><strong>整理规则</strong>：<ol><li><strong>区块排序</strong>：严格按照 <code>[versions]</code>, <code>[libraries]</code>, <code>[bundles]</code>, <code>[plugins]</code> 的顺序排列。</li><li><strong>键值排序</strong>：在每个区块内部，所有键 (key) 均按字母顺序升序排列。</li></ol></li></ul>

<p><em>使用方法</em>:</p>
<ol><li>打开 <code>libs.versions.toml</code> 文件。</li><li>在编辑器内<strong>任意位置</strong>按下 <code>Alt+Enter</code>。<ol start="3"><li>选择 <strong>Sort Version Catalog</strong> 即可。</li></ol></li></ol>

<p><strong>3. (Gradle Buddy) Update version variable to latest (更新版本变量)</strong> 🆕</p>

<ul><li><strong>痛点</strong>：<code>[versions]</code> 中的变量需要逐个手动更新。</li><li><strong>解决</strong>：放在版本变量上按 <code>Alt+Enter</code>，自动更新到最新版本。</li></ul>

<p><strong>4. (Gradle Buddy) Select other versions (选择其他版本)</strong> 🆕</p>

<ul><li><strong>痛点</strong>：需要从历史版本中选一个特定版本。</li><li><strong>解决</strong>：列出可用版本并替换 <code>version</code> 或 <code>version.ref</code>。</li></ul>

<hr />

<h2>🔄 迁移工具</h2>

<h3>Version Catalog 迁移</h3>

<p><strong>痛点</strong>：</p>
<ul><li>依赖版本散落在各个 <code>build.gradle.kts</code> 中</li><li>版本升级要改多个文件</li><li>没有统一的版本管理</li></ul>

<p><strong>解决</strong>：一键将所有硬编码依赖迁移到 <code>gradle/libs.versions.toml</code>。</p>

<h3>使用方法</h3>

<ol><li>菜单栏选择 <strong>Tools → Migrate to Version Catalog</strong></li><li>插件会：<ul><li>扫描所有 <code>.gradle.kts</code> 文件</li><li>提取硬编码依赖（如 <code>implementation(&quot;group:artifact:version&quot;)</code>）</li><li>生成/更新 <code>gradle/libs.versions.toml</code></li><li>将依赖替换为 catalog 引用（如 <code>implementation(libs.guava)</code>）</li></ul></li></ol>

<h3>迁移示例</h3>

<p><strong>迁移前</strong> (<code>build.gradle.kts</code>)：</p>
<pre><code class="language-kotlin">dependencies {
    implementation(&quot;com.google.guava:guava:33.0.0-jre&quot;)
    implementation(&quot;org.jetbrains.kotlinx:kotlinx-coroutines-core:1.8.0&quot;)
}
</code></pre>

<p><strong>迁移后</strong> (<code>gradle/libs.versions.toml</code>)：</p>
<pre><code class="language-toml">[versions]
guava = &quot;33.0.0-jre&quot;
kotlinx = &quot;1.8.0&quot;

[libraries]
guava = { group = &quot;com.google.guava&quot;, name = &quot;guava&quot;, version.ref = &quot;guava&quot; }
kotlinx-coroutines-core = { group = &quot;org.jetbrains.kotlinx&quot;, name = &quot;kotlinx-coroutines-core&quot;, version.ref = &quot;kotlinx&quot; }
</code></pre>

<p><strong>迁移后</strong> (<code>build.gradle.kts</code>)：</p>
<pre><code class="language-kotlin">dependencies {
    implementation(libs.guava)
    implementation(libs.kotlinx.coroutines.core)
}
</code></pre>

<h3>注意事项</h3>

<ul><li>已有 <code>libs.versions.toml</code> 会被合并，不会覆盖</li><li>同一依赖在不同模块版本不一致时，会显示警告</li><li>建议迁移前提交代码，便于回滚</li></ul>

<hr />

<h2>一键迁移 Project 级别依赖到 Maven 中央仓库发布过的依赖(一般是库作者会使用,模块解耦)</h2>

<p>新增功能：将 <code>project(&quot;:module&quot;)</code> 依赖迁移到 中央仓库发布过的依赖。</p>

<h3>使用方法</h3>

<ol><li>在菜单栏选择 <strong>Tools → Migrate Project Dependencies then Replace with Maven Central Dependencies</strong></li><li>或者在项目视图右键菜单中选择该选项</li><li>插件会：<ul><li>扫描所有 Gradle 文件中的 <code>project(&quot;:xxx&quot;)</code> 依赖</li><li>提取模块名作为关键词在 Maven Central 搜索</li><li>显示替换清单对话框</li><li>在对话框中选择要替换的依赖和对应的 Maven artifact</li><li>点击 OK 执行替换</li></ul></li></ol>

<h3>注意事项</h3>

<ul><li>此功能适用于将多模块项目的内部依赖迁移到已发布的 Maven 依赖</li><li>替换前请确保对应的 Maven artifact 确实是你想要的(对话框会让你选择)</li><li>建议先提交当前更改，以便于回滚</li></ul>

<hr />

<h3>🛠️ 工具窗口</h3>

<ul><li><strong>Module Tasks 面板</strong>：显示当前模块的 Gradle 任务，双击即可运行</li></ul>

<hr />

<h2>🔧 Plugin ID 修复工具</h2>

<h3>问题背景</h3>

<p>当你在 <code>build-logic</code> 中定义预编译脚本插件时，Gradle 要求使用完全限定的插件 ID：</p>

<pre><code class="language-kotlin">// build-logic/src/main/kotlin/com/example/my-plugin.gradle.kts
plugins {
    `java-library`
}

// ❌ 错误：使用短 ID 会导致运行时失败
plugins {
    id(&quot;my-plugin&quot;)
}

// ✅ 正确：必须使用完全限定 ID
plugins {
    id(&quot;com.example.my-plugin&quot;)
}
</code></pre>

<p>这个问题很难调试，因为 IDE 不会报错，只有在运行时才会失败。</p>

<h3>解决方案</h3>

<h4>1. 快速修复 (Alt+Enter)</h4>

<p>将光标放在短插件 ID 上，按 <code>Alt+Enter</code>，选择 <strong>&quot;Fix build-logic qualified name&quot;</strong>：</p>

<ul><li>自动扫描 <code>build-logic</code> 目录</li><li>提取插件的包名</li><li>在整个项目中替换所有该插件的短 ID 引用</li></ul>

<h4>2. 批量修复</h4>

<p>使用菜单 <strong>Tools → Fix All Plugin IDs in Project</strong>：</p>

<ol><li>扫描所有 <code>build-logic</code> 目录</li><li>找到所有预编译脚本插件</li><li>检测项目中所有短 ID 引用</li><li>一键替换为完全限定 ID</li></ol>

<h3>功能特性</h3>

<ul><li>✅ 自动扫描 <code>build-logic</code> 目录结构</li><li>✅ 从 Kotlin 文件提取包名</li><li>✅ 检测 <code>plugins {}</code> 块中的插件引用</li><li>✅ 项目范围的批量替换</li><li>✅ 线程安全的 PSI 访问</li><li>✅ 进度指示器和详细通知</li></ul>

<h3>使用示例</h3>

<p><strong>修复前</strong>：</p>
<pre><code class="language-kotlin">// build.gradle.kts
plugins {
    id(&quot;java-library&quot;)  // ❌ 短 ID
    id(&quot;spring-conventions&quot;)  // ❌ 短 ID
}
</code></pre>

<p><strong>修复后</strong>：</p>
<pre><code class="language-kotlin">// build.gradle.kts
plugins {
    id(&quot;com.example.conventions.java-library&quot;)  // ✅ 完全限定
    id(&quot;com.example.conventions.spring-conventions&quot;)  // ✅ 完全限定
}
</code></pre>

<hr />

<h2>快捷键汇总</h2>

<table><thead><tr><th>快捷键</th><th>功能</th></tr></thead><tbody><tr><td><code>Alt+Enter</code></td><td>在依赖上触发意图操作（更新版本等）</td></tr><tr class="intellij-row-even"><td><code>Alt+Enter</code></td><td>在插件 ID 上触发快速修复（修复为完全限定名）</td></tr></tbody></table>

<hr />

<h2>后续计划</h2>
<ul><li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled />模块白名单/黑名单</li><li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled />依赖冲突检测和解决建议</li><li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled />Plugin ID 验证和自动补全</li><li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled />支持 Groovy DSL 的插件 ID 修复</li></ul>

<hr />

<h2>Tips</h2>

<p><strong>模块睡眠功能已迁移</strong>：模块睡眠功能已迁移到独立的 <strong>Gradle Module Sleep</strong> 插件，提供更专业的按需加载和自动睡眠管理。</p>

<p>如果你需要模块按需加载和自动睡眠功能，建议使用 <strong>Gradle Module Sleep</strong> 插件。</p>]]></description>
  <depends>com.intellij.modules.platform</depends>
  <depends>org.jetbrains.kotlin</depends>
  <depends>org.jetbrains.plugins.gradle</depends>
  <!-- K2 模式兼容性声明 -->
  <extensions defaultExtensionNs="org.jetbrains.kotlin">
    <supportsKotlinPluginMode supportsK2="true" />
  </extensions>
  <!-- Intention: Update dependency to latest version (Gradle KTS) -->
  <!-- order="FIRST" 确保在intention列表中优先显示 -->
  <actions>
    <!-- Fix all plugin IDs in the project -->
    <action id="site.addzero.idfixer.FixAllPluginIdsAction" class="site.addzero.idfixer.FixAllPluginIdsAction" text="Fix All Build Script Plugin  Qualified IDs in Project" description="Scans all build-logic directories and fixes all plugin ID references to use fully qualified names.">
      <add-to-group group-id="ToolsMenu" anchor="last" />
    </action>
    <!-- Version Catalog Floating Toolbar -->
    <group id="GradleBuddy.VersionCatalogFloatingToolbarGroup" text="Version Catalog" popup="false" />
    <action id="GradleBuddy.OrganizeVersionCatalog" class="site.addzero.gradle.buddy.notification.OrganizeVersionCatalogAction" text="Organize" description="Sort and de-duplicate the version catalog" icon="/icons/gradleBannerIcon.svg">
      <add-to-group group-id="GradleBuddy.VersionCatalogFloatingToolbarGroup" anchor="first" />
    </action>
    <action id="GradleBuddy.DismissVersionCatalogToolbar" class="site.addzero.gradle.buddy.notification.DismissVersionCatalogToolbarAction" text="Don't Remind" description="Hide the version catalog floating toolbar" icon="AllIcons.Actions.Close">
      <add-to-group group-id="GradleBuddy.VersionCatalogFloatingToolbarGroup" anchor="last" />
    </action>
  </actions>
  <extensions defaultExtensionNs="com.intellij">
    <postStartupActivity implementation="site.addzero.gradle.buddy.GradleBuddyPlugin" />
    <!-- Notification Group -->
    <notificationGroup id="GradleBuddy" displayType="BALLOON" isLogByDefault="true" />
    <notificationGroup id="Gradle Plugin ID Fixer" displayType="BALLOON" isLogByDefault="true" />
    <intentionAction order="FIRST">
      <language>kotlin</language>
      <className>site.addzero.gradle.buddy.intentions.update.GradleKtsUpdateDependencyIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <intentionAction order="FIRST">
      <language>kotlin</language>
      <className>site.addzero.gradle.buddy.intentions.select.SelectOtherVersionsIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Update dependency to latest version (Version Catalog TOML) -->
    <!-- order="FIRST" 确保在intention列表中优先显示 -->
    <intentionAction order="FIRST">
      <language>TOML</language>
      <className>site.addzero.gradle.buddy.intentions.catalog.VersionCatalogUpdateDependencyIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <intentionAction order="FIRST">
      <language>TOML</language>
      <className>site.addzero.gradle.buddy.intentions.catalog.VersionCatalogSelectOtherVersionsIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Update version variable to latest (Version Catalog [versions] section) -->
    <!-- order="FIRST" 确保在intention列表中优先显示 -->
    <intentionAction order="FIRST">
      <language>TOML</language>
      <className>site.addzero.gradle.buddy.intentions.catalog.VersionCatalogUpdateVersionIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Fix version - create dedicated version ref for artifact -->
    <intentionAction order="FIRST">
      <language>TOML</language>
      <className>site.addzero.gradle.buddy.intentions.catalog.VersionCatalogFixVersionIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Convert plugin declaration to TOML format -->
    <!-- order="FIRST" 确保在intention列表中优先显示 -->
    <intentionAction order="FIRST">
      <language>kotlin</language>
      <className>site.addzero.gradle.buddy.intentions.convert.GradleKtsPluginToTomlIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Convert hardcoded plugin to catalog alias -->
    <!-- order="FIRST" 确保在intention列表中优先显示 -->
    <intentionAction order="FIRST">
      <language>kotlin</language>
      <className>site.addzero.gradle.buddy.intentions.convert.GradleKtsPluginToAliasIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Convert hardcoded dependency to TOML format -->
    <!-- order="FIRST" 确保在intention列表中优先显示 -->
    <intentionAction order="FIRST">
      <language>kotlin</language>
      <className>site.addzero.gradle.buddy.intentions.convert.GradleKtsHardcodedDependencyToTomlIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Convert version catalog reference to hardcoded dependency -->
    <intentionAction order="FIRST">
      <language>kotlin</language>
      <className>site.addzero.gradle.buddy.intentions.convert.GradleKtsCatalogDependencyToHardcodedIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Fix plugin ID to use fully qualified name -->
    <intentionAction order="FIRST">
      <className>site.addzero.idfixer.FixPluginIdIntention</className>
      <category>Gradle Plugin ID</category>
    </intentionAction>
    <!-- Intention: Select catalog reference from candidates (context menu) -->
    <intentionAction order="FIRST">
      <language>kotlin</language>
      <className>site.addzero.gradle.catalog.SelectCatalogReferenceIntentionGroup</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Intention: Browse catalog alternatives (even for valid references) -->
    <intentionAction order="NORMAL">
      <language>kotlin</language>
      <className>site.addzero.gradle.catalog.BrowseCatalogAlternativesIntention</className>
      <category>Gradle Buddy</category>
    </intentionAction>
    <!-- Inspection: Invalid catalog reference -->
    <localInspection language="kotlin" shortName="InvalidCatalogReference" displayName="无效的版本目录引用" groupName="Gradle" enabledByDefault="true" level="WARNING" implementationClass="site.addzero.gradle.catalog.InvalidCatalogReferenceInspection" />
    <!-- Completion: Gradle Kotlin Script dependencies -->
    <completion.contributor language="kotlin" implementationClass="site.addzero.gradle.buddy.intentions.completion.GradleKtsCompletionContributor" />
    <!-- Completion: Version Catalog TOML -->
    <completion.contributor language="any" implementationClass="site.addzero.gradle.buddy.intentions.completion.VersionCatalogCompletionContributor" />
    <!-- Floating toolbar for Version Catalog -->
    <editorFloatingToolbarProvider implementation="site.addzero.gradle.buddy.notification.VersionCatalogFloatingToolbarProvider" />
    <!-- Current Module Tasks Tool Window -->
    <toolWindow id="Module Tasks" anchor="right" secondary="true" icon="/icons/moduleTasksIcon.svg" factoryClass="site.addzero.gradle.buddy.filter.CurrentModuleGradleTasksToolWindowFactory" />
    <!-- Settings -->
    <projectConfigurable parentId="tools" instance="site.addzero.gradle.buddy.settings.GradleBuddySettingsConfigurable" id="site.addzero.gradle.buddy.settings" displayName="Gradle Buddy" />
    <!-- Status Bar Widget -->
    <statusBarWidgetFactory id="GradleBuddyWidget" implementation="site.addzero.gradle.buddy.GradleBuddyWidgetFactory" />
  </extensions>
</idea-plugin>
