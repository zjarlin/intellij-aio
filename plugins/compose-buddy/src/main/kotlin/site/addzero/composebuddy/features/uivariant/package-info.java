/**
 * 布尔标记转 UiVariant 能力。
 *
 * <p>适用于单个布尔标记直接驱动 if/else UI 的场景。
 * 当前第一阶段只处理单一 boolean 形参，生成 enum class 变体。
 * 触发入口：Alt+Enter Intention。
 * 当前实现状态：第一阶段可用。
 */
package site.addzero.composebuddy.features.uivariant;
