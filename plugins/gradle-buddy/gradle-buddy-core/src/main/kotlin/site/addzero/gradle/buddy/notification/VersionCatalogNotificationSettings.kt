package site.addzero.gradle.buddy.notification

/**
 * Shared constants for the version catalog helper banner.
 */
object VersionCatalogNotificationSettings {
  const val BANNER_DISABLED_KEY = "gradle.buddy.version.catalog.notification.hidden"
}
