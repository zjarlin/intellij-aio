# intellij-kit

intellij-kit is an IntelliJ IDEA plugin that packages focused Kotlin cleanup intentions and inspections.

## Included Rules

- Remove redundant explicit Kotlin property types when Kotlin can infer the same type safely.
- Default Find in Files to a source-only scope so generated directories are excluded from `Cmd+Shift+F`.
