## 2026.06.04

- Added KMP Buddy Preview Sandbox, a plugin-icon gutter entry for Compose `@Preview` functions that generates an isolated source sandbox from only the reachable Kotlin AST declarations needed by that preview.
- Added the `KMP Buddy Preview` tool window so the gutter entry opens a preview sandbox panel instead of only opening generated source files.
