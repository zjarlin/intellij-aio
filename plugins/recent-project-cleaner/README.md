Welcome-screen action that removes missing recent projects for JetBrains IDEs.

## What It Does

This plugin adds a `Clear Invalid Projects` action to the Welcome screen.
It removes recent-project entries whose local paths no longer exist, so stale projects can be cleaned in one click.
