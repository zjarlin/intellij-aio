<idea-plugin>
    <id>site.addzero.vcs-auto-update</id>
    <name>VCS Auto Update</name>
    <vendor>zjarlin</vendor>

    <description><![CDATA[
    <h3>VCS Auto Update - Auto Pull Before Git Push</h3>
    <p>Automatically pulls the latest changes from remote before you push, avoiding push conflicts.</p>

    <h4>Key Features:</h4>
    <ul>
        <li>🔄 <b>Auto Pull Before Push</b>: Automatically pulls latest changes before git push</li>
        <li>⚡ <b>Conflict Prevention</b>: Reduces merge conflicts by keeping your branch up-to-date</li>
        <li>🔔 <b>Notification</b>: Shows balloon notification when auto-pull is triggered</li>
        <li>⚙️ <b>Configurable</b>: Can be enabled/disabled in settings</li>
    </ul>

    <h4>Usage:</h4>
    <ol>
        <li>Go to Settings → Tools → VCS Auto Update</li>
        <li>Enable "Auto Pull Before Push"</li>
        <li>When you push, the plugin will automatically pull first</li>
    </ol>
    ]]></description>

    <change-notes><![CDATA[
    <p>1.0.0 - Initial release</p>
    ]]></change-notes>

    <idea-version since-build="252" until-build="262.*"/>

    <depends>com.intellij.modules.platform</depends>
    <depends optional="true" config-file="vcs-auto-update-git.xml">Git4Idea</depends>

    <extensions defaultExtensionNs="com.intellij">

        <!-- Startup Activity to initialize Git push listener -->
        <postStartupActivity
            implementation="site.addzero.autoupdate.GitPushListenerStartupActivity"/>

        <!-- Application Settings for persistent config -->
        <applicationService
            serviceImplementation="site.addzero.autoupdate.AutoUpdateSettings"/>

        <!-- Settings Page -->
        <applicationConfigurable
            parentId="tools"
            instance="site.addzero.autoupdate.AutoUpdateConfigurable"
            id="site.addzero.autoupdate.settings"
            displayName="VCS Auto Update"/>

        <!-- Notification Group -->
        <notificationGroup
            id="VSCAutoUpdate"
            displayType="BALLOON"/>
    </extensions>

</idea-plugin>
